{% extends 'base.html.twig' %}

{% block title %}AccessRight{% endblock %}

{% block body %}
    <h1>AccessRight</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ access_right.id }}</td>
            </tr>
            <tr>
                <th>Owner_type</th>
                <td>{{ access_right.ownerType }}</td>
            </tr>
            <tr>
                <th>Owner_id</th>
                <td>{{ access_right.ownerId }}</td>
            </tr>
            <tr>
                <th>Module</th>
                <td>{{ access_right.module }}</td>
            </tr>
            <tr>
                <th>Function</th>
                <td>{{ access_right.function }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('access_right_index') }}">back to list</a>

    <a href="{{ path('access_right_edit', {'id': access_right.id}) }}">edit</a>

    {{ include('access_right/_delete_form.html.twig') }}
{% endblock %}
