<form method="post" action="{{ path('access_right_delete', {'id': access_right.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ access_right.id) }}">
    <button class="btn">Delete</button>
</form>
